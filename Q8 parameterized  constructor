#include<iostream> 
using namespace std ;

class vol 
{ int x ; 
public : 
vol (int y)
{ x=y ; }
int volp ()
 { cout<<"volume is "<<x*x*x ;
  return 0 ;}
 ~vol () {} 
} ;
int main () 
{ vol A (8) ; 
A.volp () ; 
return 0  ; 
} 
